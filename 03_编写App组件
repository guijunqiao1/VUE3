由于工程入口文件对main.ts文件进行引入，由于该文件处于项目制作人编写的主战场src文件夹下，
故整个工程其实需要对main.ts文件进行功能的分析，对src文件夹中的main.ts文件内容进行分析：
import './assets/main.css'

import { createApp } from 'vue'
对于ts文件独有的引入文件的方法vue文件进行引入，并且vue本质其实是一个被暴露处理过的对象，其中有一个函数对象，该函数对象的名称为createApp，
故使用上述的解构赋值能够成功--该方法的作用介绍：
createApp方法是Vue的核心API之一,用于创建一个 Vue 应用实例,它的主要作用是初始化一个独立的应用，并将根组件与相关的功能绑定到DOM(此处为id为app的标签对象)上。

import App from './App.vue'
//App 是根组件，是 Vue 应用的起点，所有子组件都会嵌套在这个组件内部,并且通过import方法将组建默认导出为一个对象.

createApp(App).mount('#app')
创建前端的应用，并且这个应用里面的所有组件的根组件

//总结:
第一句：加载 Vue 核心功能。
第二句：加载应用的根组件。
第三句：启动 Vue 应用，将组件渲染到 DOM 中

//本节的后续内容在src目录中进行书写