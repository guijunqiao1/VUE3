6. 组件通信

Vue3组件通信和Vue2的区别：

- 移出事件总线，使用mitt代替。

- vuex换成了pinia。
- 把.sync优化到了v-model里面了。
- 把$listeners所有的东西，合并到$attrs中了。
- $children被砍掉了。

常见搭配形式：
组件关系							传递方式
父传子 						 1、props
								  2、v-model 
									3、$refs
									4、默认插槽、具名插槽

子传父 						1、props 
									2、v-model 
									3、自定义事件
									4、parent
									5、作用域插槽

祖传孙、孙传祖     1、$attrs
									2、provide-inject

兄弟间、任意组件间  1、mitt
									2、pinia

（1）限定父子之间进行信息传递：props、v-model 
（2）任意组件之间进行信息传递：mitt、pinia
（3）限定子传父：自定义事件、$parent、作用域插槽
（4）限定父传子：$refs、默认插槽、具名插槽
（5）限定祖孙之间进行信息传递：$attrs、provide-inject
（6）限定兄弟之间：无。

对上述的方式之所以能够实现对应的效果的原因做出解释：
（1）1-props：由于可以直接通过使用<Person :key:value/>的形式之后在子组件中使用defineProps方法进行key的形参的指定即可完成父到子，并且可以通过
<Person :fn:function/>的形式传递函数，并且在子组件中同样进行defineProps(['fn'])的形式进行父函数的获取之后在子组件中执行的时候传递的实参由于为
子组件中的信息并且函数体的执行是发生在父组件中的(因为是在父组件中定义的函数)，所以可以在父组件中留下key1(父组件的变量) = value1(实参)的设计即可
完成子传父；

（1）2-v-model：由于使用<Person v-model="..."/>的形式(组件标签中使用v-model属性的情况)的本质为使用了<Person :value="..." @updatemodel="...=$event"/>的情况
所以首先可以直接使用v-model="父组件中的值"的形式先完成父传子(子组件中同样使用defineProps方法进行value的接收以及defineEmits方法进行update:modelValue的接收)
,后续在子组件中可以通过进行方法的传递实参的方式完成子传父(同样和上述的函数体的执行位置以及=左边的值的归属同理)

（2）1-mitt：首先在信息源组件中可以首先进行emitter库对象的引入，并且执行emitter.on绑定方法的方式进行函数体的内容的设置(可以特意设置自身的值=外部实参)，并且
在接收信息的组件文件中首先引入“同样”的emitter对象，并且执行emitter.emit()的形式进行外部组件文件的实参的传递，最终完成当前组件到别的组件的信息传递

（2）2-pinia：首先要知道pinia的store库对象可以通过useStore方法进行库对象的获取，并且执行useStroe()方法可以将库对象内容直接取出，后续直接使用上x.数据.pop()方法进行
当前组件的信息的存入即可，并且接收信息的组件则进行同样的方法将库对象内容进行取出即可；同时若需要反向传递则再创建一个专门用于单向信息传递的ts文件文件即可（因为无法再外部文件
中直接通过获取到库对象之后对原先的库进行新的响应式数据的添加），然后执行相同的操作流程，多个单向库文件组成双向。

（3）1-自定义事件：假设自定义事件的定义以及开始传递发生在父组件身上，则由于自定义事件的函数体内容都带有$event--表示形参占位，对于接收信息的组件需要使用defineEmits方法进行
自定义事件的接收，并且还需要使用变量进行返回值的接收，否则没有介质执行该自定义事件，并且可以使用变量名(自定义事件名，实参)的形式执行该自定义函数内容，并且由于实参来源于
子组件，并且函数体内容可以设计为=左边为父组件的变量，即可达成子传父的目的。

（3）2-$parent：首先$parnet这个东西表示的含义就是引入了当前的文件作为组件的距离当前位置最近的组件标签对象，同样在当前文件中定义函数内容，形参为外部文件对象并且处于=左边
，=右边为当前文件的变量内容，在执行函数的时候则传入实参为$parent即可达成子传父，=互换同样可以完成父传子。

（3）3-作用域插槽：可以在子组件中的<template>标签中使用上<slot :key:value><slot/>的形式发出信息，父组件中的组件双标签中的template标签中的v-slot插头若和发出了信息的插槽
对应的话则在那个插头的范围内该信息成功完成了传递，并且父元素可以进行value的内容使用，完成了子传父。

（4）1-$refs：首先$refs这个东西表示的含义就是当前文件的使用的子组件中带有ref属性标识的所有的标签对象本身所组成的对象，则可在当前组件(相对而言的父组件)中定义函数，内容为
=左边为子组件中的变量内容(由形参进行获取到)并且=右边为当前文件中的内容，在调用该函数的时候实参传递为$refs即可完成父传子，其实也可以通过=左右元素互换完成子传父。

（4）2-默认插槽：首先插槽的本质是被插口内容填充并且替换掉组件标签整体，而插头(template)在父组件中，并且在插头设置关于父组件的变量内容(准备发出的信息)，同时插槽(slot)在子组件中
，则在进行程序的执行的时候则自然而然完成了父传子。

（4）3-具名插槽：具名插槽的本质只是在原先默认插槽的情况下多了一个插头需要严格和指定的插槽进行匹配插入，所以同样的原理完成了父传子。

（5）1-$attrs：首先需要知道$attrs本身表示的是开发者工具中的attrs目录下的所有的键值对所组成的对象，在父组件中进行:key="value"的Props传递的时候则优先被放置在attrs目录下，
并且如果子组件没有使用defineProps方法将对应的键值对进行取出的话，则会一直保留在attrs目录下，反之则全在Props目录下并且可以在当前子组件中进行直接的使用，若需要继续将最初传递过来的
键值对进行传递则直接使用<GrandChild v-bind="$attrs"/>的形式将对象进行传递，并且由于v-bind会拆解对象为单纯的键值对，则正常进行内容的传递，最终在目的地孙组件中则可正常使用defineProps方法
将信息接收，完成祖传孙，并且若传递的有函数，则同样在孙组件中执行的使用传入孙组件中的变量内容的时候再返回到祖组件中执行的时候进行内容的设计故可完成孙传祖。

（5）2-provide-inject：首先在当前组件中执行porvide方法可将信息内容进行提交，后续的子组件中若使用了inject即可将信息直接进行接收，故完成了祖传孙，同样也可以从孙组件
开始使用provide方法进行信息的提供，从上开始查找（查找的依据的子组件引用链<Person/>）,同理即可完成孙传祖。

; 后三种之所以没有成为任意组件的通信类型的原因在于都需要依赖子组件<Person/>的参与

6.1. 【props】

概述：props是使用频率最高的一种通信方式，常用与 ：父 ↔ 子。

- 若 父传子：属性值是非函数。
- 若 子传父：属性值是函数。

父组件：
<template>
  <div class="father">
    <h3>父组件，</h3>
		<h4>我的车：{{ car }}</h4>
		<h4>儿子给的玩具：{{ toy }}</h4>
		<Child :car="car" :getToy="getToy"/>
  </div>
</template>

<script setup lang="ts" name="Father">
	import Child from './Child.vue'
	import { ref } from "vue";
	// 数据
	const car = ref('奔驰')
	const toy = ref()
	// 方法
	function getToy(value:string){
		toy.value = value
	}
</script>
子组件：
<template>
  <div class="child">
    <h3>子组件</h3>
		<h4>我的玩具：{{ toy }}</h4>
		<h4>父给我的车：{{ car }}</h4>
		<button @click="getToy(toy)">玩具给父亲</button>
  </div>
</template>

<script setup lang="ts" name="Child">
	import { ref } from "vue";
	const toy = ref('奥特曼')
	
	defineProps(['car','getToy'])
</script>


















