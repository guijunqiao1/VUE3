setup 的返回值--对应备份文件夹中的04_setup的返回值

- 若返回一个对象：则对象中的：属性、方法等，在模板中均可以直接使用（重点关注）。
- 若返回一个函数：则可以自定义渲染内容，代码如下：
setup(){
  return ()=> '你好啊！'
}
解释说明：所谓的自定义地进行内容的渲染指的是：由于本质上setup为一个被export的部分，所以当其他的枝文件进行当前文件内容的引入的时候
直接将函数的返回值的内容替代整个组件的内容进行传递